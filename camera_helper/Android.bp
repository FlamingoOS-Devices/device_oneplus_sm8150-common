android_app {
    name: "OnePlusCameraHelper",

    srcs: ["src/**/*.java"],

    certificate: "platform",
    platform_apis: true,
    privileged: true,

    resource_dirs: ["res"],
    static_libs: [
        "androidx.annotation_annotation",
    ],
}
